[project]
name = "city-population-with-lat-lng"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.35.2",
    "rich>=14.2.0",
]
